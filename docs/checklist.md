# checklist.md — 4주 작업 체크리스트

> 매장 맞춤 알바 훈련 서비스 MVP (업종: 카페 1개로 축소)
>
> 쪼갠 3축:
> - **레이어 축**: 데이터·로직(백엔드) / UI(프론트) — 1주차부터 **두 트랙을 병행**하고, mock을 실제 API로 교체하며 합류시킨다. 8개 화면 목업(`ui/1_업종선택.png` ~ `ui/8_사장님리포트.png`)이 이미 나와 있으므로, UI는 로직 완성을 기다리지 않고 mock 데이터로 먼저 코딩한다. *(2026-07-13 기준: 1주차 계획한 분량을 다 못 끝내 백엔드 코어·UI 컴포넌트화·mock→API 교체 모두 2주차로 이월하고 요일별 일정(7/13~7/17)으로 재배분함 — docs/backlog.md 5절 참고. 단 UI 화면 라우팅 골격·Railway/Supabase/Vercel 배포는 3주차로 유지)*
> - **중요도 축**: 코어(A 대화 + B 평가)를 먼저, 설정·리포트는 뒤에
> - **완성도 축**: 뼈대(돌아가게) → 정확도(믿을 만하게) → 다듬기(써볼 만하게)
>
> 우선순위 원칙: **(1) 대화+평가 코어 → (2) 규칙 입력 → (3) 리포트(시간 남으면).**
> 가장 어려운 것(규칙→루브릭 변환, 평가 에이전트 신뢰성)을 앞 주차로 당긴다.

---

## 1주차 — 뼈대 & 데이터 + UI 목업 코딩 (두 트랙 병행)

**목표: 트랙 A(백엔드)는 데이터베이스를 만들고, 가장 어려운 "규칙 → 루브릭 변환"이 돌아가게 만든다. 트랙 B(프론트)는 8개 화면을 mock 데이터로 눌러볼 수 있게 만든다. 두 트랙은 따로 진행하되, 데이터 형태(JSON 모양)만 미리 맞춰둔다.**

> DB는 PostgreSQL, 스키마·ERD는 db-schema.md 참고. 아래 6개 테이블(`stores`, `store_rules`, `scenarios`, `rubrics`, `training_sessions`, `session_turns`)을 그대로 가져와 쓴다.

### 트랙 A — 데이터·로직 (백엔드)

#### Day 1 — 프로젝트 셋업 & 배포 대상 만들기
- [ ] 레포 초기화 (React + Express, 프론트/백엔드 디렉터리 분리)
- [ ] `.env` 설계·관리 (Anthropic API 키, `DATABASE_URL`, `.env.example` 커밋)
- [ ] **Railway 프로젝트 생성** — Express 백엔드 호스팅 (plan.md 5-5)
- [ ] **Supabase 프로젝트 생성** — Postgres DB 호스팅 (2026-07-13 과제 요구사항에 맞춰 Railway Postgres→Supabase로 전환, docs/db-migration-plan.md 참고)
- [ ] **Vercel 프로젝트 생성 및 GitHub 연결** — 프론트(React)용, push마다 자동 배포되는지 확인
- [ ] ORM 셋업 — Prisma 권장 (`npx prisma init`), db-schema.md의 스키마를 `schema.prisma`로 옮기기
- [ ] Anthropic API 연결 스모크 테스트 (키 유효성만 확인하는 최소 호출 1개)
- [ ] 빈 화면·빈 서버 상태로 한 번 배포해보기 — Railway(빈 서버)·Supabase(빈 DB 프로젝트)·Vercel(빈 화면) 전부 연결된 것만 확인 (내용은 나중에 채움)

#### Day 2 — 데이터 모델 마이그레이션 & 매장 링크 API
> **1주차 안에 끝내지 못해 2주차로 이월** — 아래 "2주차 — 코어 로직 + UI 컴포넌트화" 섹션의 "7/13 — DB 마이그레이션 + 매장 링크 API"에서 이어서 진행 (docs/backlog.md 5절 참고).

#### Day 3~4 — 규칙 → 루브릭 변환 (가장 어려운 것, 최우선)
> **1주차 안에 끝내지 못해 2주차로 이월** — 아래 "2주차 — 코어 로직 + UI 컴포넌트화" 섹션의 "7/14 — 규칙 → 루브릭 변환 로직"에서 이어서 진행. eval 셋 구축은 "7/15"로 이어서 진행.

**트랙 A 1주차 완료 기준**: (미달성 — 2주차로 이월) 규칙을 텍스트로 넣으면 → `store_rules`에 원문이 저장되고 → 변환된 루브릭 JSON이 `rubrics` 테이블에 찍힌다 (DB·콘솔 확인 수준으로 충분, 화면 연결은 2주차).

### 트랙 B — UI (프론트, mock 데이터로 8개 화면 코딩)

- [x] **HTML 클릭스루 프로토타입 완성** (2026-07-09) — `ui/prototype/`에 9개 화면을 정적 HTML로 구현하고 전부 클릭 흐름 연결: `landing_light.html`(메인) → `industry_select.html`(업종 선택) → `rules_preview.html`(규정 예시 확인) → `rules_confirmed.html`(규정 확정) → `staff_invite.html`(알바 초대) → `training_session.html`(AI 훈련 세션) → `feedback_report.html`(피드백 리포트) → `my_progress.html`(알바 개인 현황), 그리고 `dashboard.html`(사장님 대시보드) 경로까지. *React 컴포넌트는 아니며, 아래 컴포넌트화 항목과는 별개.*
- [x] 마스코트 캐릭터(`hub/img/mascot-{greeting,approve,coach,confused}.png`) 4종 적용 및 경로 정리 완료
- [x] 디자인 시스템 토큰화 완료 — `.claude/skills/default-design/SKILL.md` + `docs/design.md`(컬러·폰트·모서리·여백 토큰, 마스코트 사용 규칙)

> React 컴포넌트화·mock fixture 작성·화면 간 라우팅 연결은 **1주차 안에 끝내지 못해 2주차로 이월** — 아래 "2주차 — 코어 로직 + UI 컴포넌트화" 섹션의 "7/16 — 손님 에이전트 + UI 컴포넌트화"에서 이어서 진행. 사장님/알바 경로 분리(라우팅 골격)만 3주차로 별도 이월 (docs/backlog.md 5절 참고).

**트랙 B 1주차 완료 기준**: (미달성 — 2주차로 이월) 로그인 없이 8개 화면을 순서대로 클릭해 전체 흐름을 mock 데이터로 끝까지 볼 수 있다. HTML 프로토타입(`ui/prototype/`)으로는 이미 가능하지만, React 컴포넌트로는 아직이다.

---

## 2주차 — 코어 로직 + UI 컴포넌트화 (요일별 일정, 7/13~7/17)

**목표: 이번 주 안에 "매장 링크 발급 → 규칙 저장 → 루브릭 변환 → 손님과 대화 → 평가"가 API/콘솔 레벨로 돌아가는 것과, UI 8개 화면 React 컴포넌트화·mock→API 교체까지 요일별로 나눠 전부 끝낸다. 1주차 이월분(매장 링크 API, 규칙→루브릭 변환, UI 컴포넌트화)이 다른 작업의 전제이므로 먼저 처리한다 (docs/backlog.md 5절 참고, Notion 표 기준 요일별 배분). UI 화면 라우팅 골격(권한 분리)·Railway/Supabase/Vercel 배포는 여전히 3주차 몫이다.**

### 7/13 (월) — DB 마이그레이션 + 매장 링크 발급/조회 API `완료`
- [x] 6개 테이블 마이그레이션 실행 (`stores` / `store_rules` / `scenarios` / `rubrics` / `training_sessions` / `session_turns`) — Railway Postgres에 `prisma migrate dev` 적용 완료 (`docs/db-migration-plan.md` 참고, **Supabase로 전환 후 재적용 필요** — 2026-07-13)
  - [x] `stores.link_key` UNIQUE 제약 확인 — *로그인 없는 식별의 유일한 키*
  - [x] `rubrics.approved_at` nullable 확인 — *승인 전 초안 상태를 스키마로 표현*
- [x] 매장 링크 발급 API: `POST /api/stores` → `link_key` 생성해 반환 (`server/src/routes/stores.js`)
- [x] 매장 조회 API: `GET /api/stores/:linkKey` → 없으면 404 (`server/src/routes/stores.js`)
- [x] Prisma Client로 6개 테이블 CRUD 스모크 테스트 (임의 데이터 삽입·조회 1회씩)

### 7/14 (화) — 규칙 → 루브릭 변환 로직 (가장 어려운 것)
- [ ] **규칙 → 루브릭 변환 LLM 호출** ★ 최난이도 — **Claude Sonnet 5** 사용 (호출 빈도 낮음, 품질 우선. plan.md 5-3)
  - [ ] 프롬프트 설계: "이 매장 규칙을 평가용 체크리스트로 쪼개라. 각 항목 must/nice 구분, 좋은예·나쁜예 포함"
  - [ ] 출력 JSON 스키마 강제: `[{item, required, good_example, bad_example}]` — `rubrics.criteria` 컬럼 형식과 그대로 맞추기
  - [ ] 변환 결과를 `rubrics` 테이블에 저장 (`approved_at = null`인 초안 상태로)
  - [ ] 카페 시나리오 3개(지연·품절·규칙위반)에 대해 손으로 규칙 예시 텍스트를 넣고 루브릭이 뽑히는지 확인
  - [ ] 실패 케이스 1차 점검: 규칙이 모호하거나 짧을 때 루브릭이 비거나 이상해지지 않는지
- [ ] 업종 템플릿(질문 세트) 정의 — 카페용: 환불/교환·지연·품절·금지표현 질문 목록 확정
- [ ] 규칙 원문 저장 API: `POST /api/stores/:linkKey/rules` → `store_rules`에 자연어 원문 저장 후 변환 호출 트리거
- [ ] **트랙 B와 API 응답 포맷 합의** — 루브릭 `criteria`, 평가 `evaluation` JSON 모양을 이 시점에 확정해 공유 (7/16 UI 컴포넌트화·7/17 mock→API 교체가 어긋나지 않도록)

### 7/15 (수) — eval 셋 구축
- [ ] **eval 셋 구축** ★ (규칙 텍스트, 기대 루브릭) 쌍 10~15개를 미리 만들어 저장 — 프롬프트를 바꿀 때마다 감이 아니라 이 셋으로 회귀 확인. 이후 평가 에이전트 신뢰성 검증(3주차)에도 같은 방식 재사용

### 7/16 (목) — 손님 에이전트 + UI 8개 화면 React 컴포넌트화
- [ ] **손님 에이전트 대화 LLM 호출**
  - [ ] 시스템 프롬프트: 페르소나 + 시나리오 상태(주문상태·손님기분·경과턴) 주입
  - [ ] **Claude Haiku 4.5** 사용 (호출이 잦으므로 빠르고 저렴한 모델)
  - [ ] 대화 히스토리를 매 호출에 전달 (상태 유지 — LLM은 기억이 없음)
  - [ ] **분기 처리**: 알바 답변에 따라 손님 반응이 달라지게 (1회성 챗봇과 구분되는 지점)
- [ ] **UI 8개 화면을 목업(`ui/1~8_*.png`) 그대로 React 컴포넌트화** (mock 데이터, HTML 프로토타입은 완료·React 전환은 아직)
  - [ ] 1. 업종선택
  - [ ] 2. 규칙입력 (자연어 textarea + 업종 템플릿 질문 표시)
  - [ ] 3. 루브릭승인 ("이렇게 정리했어요, 맞나요?" + 승인/수정)
  - [ ] 4. 매장링크전달
  - [ ] 5. 시나리오선택 (카드 3개: 지연·품절·규칙위반)
  - [ ] 6. 훈련대화 (턴제 채팅 UI + 인라인 체크리스트 자리)
  - [ ] 7. 훈련결과요약
  - [ ] 8. 사장님리포트
- [ ] 화면별 mock 데이터 fixture 작성 — 7/14에 확정한 API 응답 JSON 포맷 그대로 사용 (예: 루브릭 criteria, 평가 evaluation)
- [ ] 화면 간 라우팅 연결 (규칙입력→루브릭승인→매장링크전달, 시나리오선택→훈련대화→훈련결과요약)

### 7/17 (금) — 평가 에이전트 + 턴 루프 + 세션 저장 + UI mock→API 교체
- [ ] **평가 에이전트 LLM 호출** ★ 신뢰성 핵심
  - [ ] 입력: (루브릭, 시나리오 맥락, 대화 히스토리, 알바 답변)
  - [ ] **Claude Sonnet 5** 사용 (판단이 신중해야 하므로)
  - [ ] **출력 JSON 강제**: `{충족여부, 빠진기준[], 피드백, 개선문장}` — 마크다운·서두 금지
  - [ ] 근거를 규칙 인용으로 대게 ("②번 미충족: 'ETA 안내' 규칙")
- [ ] A와 B를 하나의 턴 루프로 연결 (손님 발화 → 알바 답변 → 평가 → 분기)
- [ ] **미충족 → 재입력 루프**: 개선 문장 제시 후 같은 턴 다시 시도
- [ ] 충족 → 다음 턴 또는 훈련 완료 처리
- [ ] 훈련 세션 저장 (`training_sessions`/`session_turns`에 대화·답변·평가·재시도 실제 기록 시작)
- [ ] **UI mock → 실 API 교체**
  - [ ] 규칙입력·루브릭승인 화면(2·3번)을 규칙→루브릭 변환 API와 연결 (mock 텍스트 대신 실제 제출)
  - [ ] 매장링크전달 화면(4번)을 실제 링크 발급 API와 연결
  - [ ] 훈련대화 화면(6번)의 mock 대화를 실제 손님 에이전트 API로 교체 — *현재 `training_session.html` 프로토타입은 하드코딩된 대화 예시만 있음*
  - [ ] 훈련대화 화면의 mock 평가 결과를 실제 평가 에이전트 API로 교체

**2주차 완료 기준**: 매장 링크로 규칙을 저장하면 루브릭이 생성되고, 시나리오 대화 API를 몇 턴 호출하면 손님 응답과 평가(통과/미통과 + 개선문장)가 실제로 JSON으로 돌아온다. 동시에 8개 화면이 React 컴포넌트로 존재하고, mock 대신 실제 API로 규칙입력·루브릭승인·매장링크전달·훈련대화가 동작한다. UI 화면 라우팅 골격(권한 분리)·Railway/Supabase/Vercel 배포는 3주차 몫으로 남는다.

---

## 3주차 — 신뢰성 & UI 디테일 (믿을 만하고 실사용에 안전하게)

**목표: UI 컴포넌트화·mock→API 교체는 2주차에서 끝났으므로, 3주차는 화면 라우팅 골격(사장님/알바 경로 분리)을 마무리하고 평가 에이전트를 신뢰할 만하게 다듬으며, 목업엔 없던 실사용 엣지 상태(로딩·에러·빈 값)까지 버티게 만드는 데 집중한다. 여기에 코드스쿼드 3주차 공식 미션 요구사항(아키텍처 시각화, 테스트/TDD, AI 설계 기반 기능 강화, Agent 산출물)을 반영한다 — 상세 근거는 [backlog.md 8절](./backlog.md#8-3주차까지-끝낼-것-코드스쿼드-공식-미션-요구사항-반영-2026-07-20) 참고.**

### 7/20 (월) — 라우팅 골격 + 테스트 기반 + 아키텍처 문서 + 기능강화① 설계
- [ ] 화면 라우팅 골격 (사장님 경로 / 알바 경로 분리) — *현재 HTML 프로토타입·2주차 React 컴포넌트는 정적 링크 클릭 이동만 가능, 실제 역할 구분·권한 체크는 아직 없음*
- [ ] Vitest 설정 (루트 또는 server, CLAUDE.md에 예고돼 있던 도입 시점)
- [ ] README에 mermaid 다이어그램 추가 — 화면→서버→DB 데이터 흐름 (규칙 기반 AI 시나리오 생성, AI 손님과 실전 대화 두 흐름). `docs/db-erd.md`의 ERD와는 별개(그건 테이블 관계도만 다룸)
- [ ] **기능 강화 ①** 루브릭 재사용성 — AI와 설계(task-planner) → 작게 나눠 이슈 등록. 업종·규칙이 비슷한 매장끼리 루브릭 템플릿을 캐싱/공유해 Gemini 호출 자체를 줄임 (멘토링 피드백, [backlog.md 7절](./backlog.md#7-멘토링-반영-2026-07-20) 참고)

### 7/21 (화) — 기능강화① 구현(TDD) + 테스트 Skill + 기능강화② 설계
- [ ] 기능 강화① 구현 — **TDD로 진행** (테스트 먼저 작성 → 구현). 단위/통합 테스트 요구사항을 이 기능으로 충족
- [ ] 테스트코드 생성 Skill 제작 (`.claude/skills/`) — 기능강화①에서 짠 테스트 패턴을 재사용 가능한 형태로 뽑아냄
- [ ] **기능 강화 ②** 평가 결과 교차검증 — AI와 설계 → 작게 나눠 이슈 등록. 평가 에이전트(Sonnet 5)가 채점한 결과를 다른 모델(예: GPT)로 한 번 더 검증하는 자동화 루프 (고스트캠프 멘토링 피드백, [backlog.md 6절](./backlog.md#6-멘토링-반영-2026-07-13) 참고)

### 7/22 (수) — 기능강화② 구현 + 평가 캘리브레이션 + 워크플로우 문서
- [ ] 기능 강화② 구현
- [ ] **평가 에이전트 캘리브레이션** ★ (human-in-the-loop)
  - [ ] 사장님이 초기 평가 결과 몇 개를 검토·교정하는 화면/흐름
  - [ ] 오답 케이스 모아 루브릭·프롬프트 다듬기
  - [ ] 엣지 케이스 테스트 (빈 답변, 무관한 답변, 규칙에 없는 얘기)
- [ ] 나만의 Agent 워크플로우 문서화 — task-planner(계획) → 구현 → feature-verifier(검증) 흐름
  - [x] 코드 검증 Agent 제작 및 활용 — `feature-verifier`(`.claude/agents/feature-verifier.md`), 시나리오 생성 기능에 실제 적용 완료 (7/17)

### 7/23 (목) — 안정성 보강 + UI 디테일 + 배포 + 최종 검증
- [ ] 시나리오 배치 생성 트랜잭션으로 묶기 (`prisma.$transaction`) — *`stores.js`의 `POST /:linkKey/rules`가 `Promise.all`로 여러 개를 만드는데, 중간 실패 시 부분 데이터가 남을 수 있음*
- [ ] AI 응답 JSON 파싱 방어 (`evaluator.js`/`rubric.js`/`scenarioProposer.js`) — *`responseSchema` 강제해도 100% 보장 아님, "[필수]" 접두어 버그가 그 증거*
- [ ] AI 호출 재시도 로직 (Gemini/OpenAI 호출 실패 시 1~2회, backoff)
- [ ] eval 셋을 시나리오 제안·평가(evaluator)에도 확장 — *지금 `server/eval/rubric-eval-set.json`은 루브릭 생성만 커버*
- [ ] AI 호출 성공/실패·소요시간 로깅 — *지금은 `console.error`뿐이라 실패 패턴을 사후에 알 방법이 없음*
- [ ] 훈련대화 화면: 로딩 상태(에이전트 응답 대기 중 표시), 응답 지연 시 처리
- [ ] 훈련대화 화면: 항목별 ✓/✗ 표시가 실제 평가 JSON과 정확히 매핑되는지 (`✓ 사과 / ✗ 대기시간 안내 / ✓ 금지표현 없음`)
- [ ] 루브릭승인 화면: 승인 루프 실동작 확인 (수정 요청 → 재생성 → 다시 승인)
- [ ] 빈 답변·네트워크 오류 등 실패 상태 UI
- [ ] 반응형/모바일 점검 — *알바는 매장 링크를 폰으로 열 가능성이 높음*
- [ ] 대시보드 알바 목록 등 클릭형 UI 접근성 개선 — *현재 프로토타입은 `<tr onclick>`만 있어 키보드 탐색이 불가능, 포커스 가능한 요소(버튼/링크)로 전환 필요*
- [ ] Railway(BE)/Supabase(DB)/Vercel(FE) 배포 — *1주차 Day 1에 끝내지 못해 이월, 금요일 발표 전 하루 여유를 두기 위해 목요일에 배치, 안정성 보강 이후에 배포*
- [ ] `feature-verifier`로 전체 흐름 최종 검증
- [ ] PR 정리

### 7/24 (금) — 발표 데모
- [ ] 발표 데모 (개발 작업 없음, 버그 나오면 최소 수정만)

**3주차 완료 기준**: 사장님이 규칙 넣고 루브릭 승인 → 알바 계정 생성(이메일+초기 비밀번호) → 알바가 로그인해 대화·교정까지 끊김 없이 흐른다. 사장님/알바 경로가 분리되어 있고, 평가가 대체로 믿을 만하며, 에러 상황에서도 화면이 깨지지 않는다. 루브릭 재사용·교차검증 기능이 실제로 동작하고, README의 mermaid 다이어그램으로 전체 구조를 설명할 수 있으며, 테스트(Vitest)가 최소 1개 기능을 커버한다.

---

## 4주차 — 리포트 데이터 · MCP 전환 · 배포 · QA

**목표: 전체 흐름을 QA하고, 이미 만들어진 사장님리포트 화면에 실제 데이터를 채우고, 데이터 접근을 MCP로 전환한 뒤 배포한다.**

### 리포트 데이터 채우기
- [ ] 사장님리포트 화면(8번)에 실제 세션 집계 쿼리 연결 (알바별 훈련·약점 리포트) — *화면은 1주차에 이미 있음, 데이터만 채우면 됨*

### MCP 전환 (에이전트–데이터 연결, plan.md 5-6)
- [ ] MCP 서버 프로젝트 셋업, tool 4개 정의 (`get_store_rules`, `get_rubric`, `save_session_turn`, `get_session_report`)
- [ ] 손님 에이전트가 `get_store_rules`·`get_rubric`을 tool call로 쓰도록 교체
- [ ] 평가 에이전트가 `get_rubric`·`save_session_turn`을 tool call로 쓰도록 교체
- [ ] 기존 직접 쿼리 방식과 비교해 정상 동작하는지 회귀 확인 (1주차 eval 셋 재사용)

### 배포 (Day 1에 만든 대상에 실제로 올리기)
- [ ] Express 백엔드 Railway에 배포, 환경변수(`DATABASE_URL`은 Supabase 프로덕션 값, Anthropic API 키) 프로덕션 값으로 설정
- [ ] React 프론트 Vercel에 배포, 프로덕션 API 주소로 연결
- [ ] 프로덕션 URL로 실제 접속해서 "규칙 입력 → 훈련 → 리포트" 전체 흐름 한 번 확인 (로컬 아닌 배포본 기준, DB는 Supabase)

### QA & 마감
- [ ] 전체 흐름 통합 테스트 (사장님 설정 → 알바 훈련 → 결과 확인)
- [ ] 매장 링크 접속·로그인 없는 경로 점검
- [ ] 발표 시나리오용 데모 데이터(카페 예시) 세팅
- [ ] 경쟁 비교표·문제정의 슬라이드 정리 (plan.md 1-4 활용)

### 여유 시 (시간 부족하면 가장 먼저 버릴 항목)
- [ ] 규칙·시나리오 초안 자동 생성 (업종 템플릿 → AI 초안), AI 초안 승인/수정 UX 다듬기
- [ ] **매뉴얼 사진 → 규칙 초안** — 별도 OCR 서비스 없이 **Claude Sonnet 5의 비전 입력**으로 사진을 바로 읽어 규칙 텍스트를 뽑는다 (plan.md 5-3)

**4주차 완료 기준**: Railway(BE)/Vercel(FE)에 배포된 URL로 접속해서 "규칙 입력 → 훈련 → 리포트"가 한 번에 보여지는 완결된 v1 (DB는 Supabase). 데이터 접근은 MCP로 전환되어 있다.

---

## 축별 요약 (한눈에)

| 주차 | 레이어 무게중심 | 중요도 | 완성도 |
|---|---|---|---|
| 1주 | 트랙 A(데이터+난이도 높은 로직) / 트랙 B(UI 목업 코딩) 병행 | 코어 토대(루브릭 변환) + 전체 화면 뼈대 | 뼈대 |
| 2주 | 백엔드 코어(A+B) + UI 컴포넌트화·mock→API 교체 (요일별 일정, 1주차 이월분 포함) | **코어 + UI 합류** | 뼈대→돌아감 |
| 3주 | UI 라우팅 골격 + 정확도·디테일 | 코어 신뢰성 | 정확도 |
| 4주 | 리포트 데이터·MCP 전환·배포·QA | 마감 + MCP | 다듬기 |

## 리스크 메모 (막히면 여기부터)
- 루브릭 변환이 부정확 → 1주차에서 막힘. 프롬프트에 좋은예/나쁜예를 더 구체화.
- 평가 에이전트가 오채점 → 2~3주차 핵심 리스크. 루브릭을 더 이분법적으로, 근거 인용 강제, 사장님 캘리브레이션.
- 트랙 A/B 데이터 포맷 불일치 → 2주차 초반(규칙→루브릭 변환이 끝나는 7/14)에 JSON 스키마를 확정해 UI 컴포넌트화(7/16)·mock→API 교체(7/17) 전에 반드시 공유. 형식이 어긋나면 같은 주 안에 못 끝난다.
- 배포는 4주차에 처음 하지 않는다 → 1주차 Day 1에 빈 껍데기로 미리 올려 파이프라인을 검증해둔다(환경변수·CORS 사고 예방). 못 끝냈으면 3주차에 반드시 이어서 처리.
- MCP 전환이 늦어짐 → 4주차 핵심 로직은 이미 순수 API로 도는 상태이므로, MCP는 "동작하는 v1 위에 얹는" 순서. 코어(A+B)·배포가 끝나기 전엔 MCP에 손대지 않는다.
- 시간 부족 → **자동화 UX·사진 입력부터 버린다.** 코어(A+B)와 규칙입력은 절대 안 버림.
