# backlog.md — 개발 Task 백로그

> 2주차부터 본격 개발이 시작된다. 뭘 언제 할지 미리 정해두면 개발 중에 흔들리지 않는다.
> 상세 근거·전체 맥락은 [plan.md](./plan.md), 세부 작업 단위는 [checklist.md](./checklist.md) 참고 — 이 문서는 그 둘을 **Task 백로그 형태로 압축**한 것.

---

## 1. 백로그란?

- **정의**: 해야 할 일을 전부 모아둔 하나의 목록. "언젠가 할 일"까지 다 적어두고, 그중 우선순위 높은 것부터 꺼내 쓴다.
- **왜 필요한가**: 할 일이 머릿속이나 대화에만 있으면 놓치거나 순서가 흔들린다. 한곳에 모아두면 "지금 뭐부터 해야 하지?"를 매번 고민하지 않아도 된다.
- **어떻게 생겼나**: Task 이름 + 우선순위 + 상태(백로그/진행중/완료) + 어느 주차 것인지, 이 정도 컬럼만 있는 단순한 표. 화면 스펙처럼 세세하게 쓰지 않는다 — 그건 checklist.md 같은 별도 문서 몫.
- **어떻게 관리하나**: 지금은 이 마크다운 파일로 관리하고, 이후 Notion 데이터베이스로 그대로 옮긴다. 매주 시작할 때 이번 주에 할 항목을 위쪽으로 올리고, 끝나면 상태를 "완료"로 바꾼다.

---

## 2. 우선순위 · 상태 표기 규칙

| 표기 | 의미 |
|---|---|
| **필수** | 없으면 서비스가 성립 안 함 (코어: A 대화 + B 평가) |
| **중요** | 있어야 쓸 만해짐 (규칙 입력, 신뢰성, 배포) |
| **여유** | 시간 남으면 (리포트, 자동화, 디테일) |

상태는 `백로그`(아직 시작 전) → `진행중` → `완료` 세 가지만 쓴다.

---

## 3. Task 목록 (큰 단위)

| Task | 날짜 | 주차 | 우선순위 | 상태 |
|---|---|---|---|---|
| DB 스키마 (Prisma) | — | 1주 | 필수 | 완료 |
| DB 마이그레이션 실행 (6개 테이블) | 7/13 | 2주 | 필수 | 완료 |
| 매장 링크 발급/조회 API (POST /api/stores 등) | 7/13 | 2주 | 필수 | 완료 |
| 규칙 → 루브릭 변환 로직 (LLM 호출) | 7/14 | 2주 | 필수 | 백로그 |
| eval 셋 구축 (규칙-루브릭 쌍) | 7/15 | 2주 | 중요 | 백로그 |
| 백엔드 개발 환경(Express+Prisma) 셋업 | — | 1주 | 필수 | 완료 |
| Railway(BE)/Supabase(DB)/Vercel(FE) 빈 껍데기 배포 | — | 3주 이월 | 중요 | 백로그 |
| UI 8개 화면 클릭스루 프로토타입 | — | 1주 | 필수 | 완료 |
| 디자인 시스템 토큰화 | — | 1주 | 중요 | 완료 |
| UI 화면 라우팅 골격(사장님/알바 경로 분리) | — | 3주 이월 | 중요 | 백로그 |
| UI 8개 화면 React 컴포넌트화 (mock 데이터) | 7/16 | 2주 | 필수 | 백로그 |
| 손님 에이전트 대화 로직 (Haiku 4.5) | 7/16 | 2주 | 필수 | 백로그 |
| 평가 에이전트 로직 (Sonnet 5, JSON 강제) | 7/17 | 2주 | 필수 | 백로그 |
| 두 에이전트 턴 루프 연결 + 재입력 루프 | 7/17 | 2주 | 필수 | 백로그 |
| 훈련 세션 DB 저장 | 7/17 | 2주 | 필수 | 백로그 |
| UI mock → 실제 API 교체 | 7/17 | 2주 | 필수 | 백로그 |
| 평가 에이전트 캘리브레이션(사장님 검토) | — | 3주 | 중요 | 백로그 |
| 평가 결과 교차검증 (타 모델로 재검증) | — | 3주 | 중요 | 백로그 |
| 로딩·에러·빈 값 등 UI 엣지 상태 처리 | — | 3주 | 중요 | 백로그 |
| 반응형/모바일 점검 | — | 3주 | 중요 | 백로그 |
| 사장님 리포트 데이터 연결 | — | 4주 | 여유 | 백로그 |
| MCP 서버 전환 (4개 tool)¹ | — | 4주 | 중요 | 백로그 |
| 프로덕션 배포 (Railway+Supabase+Vercel) | — | 4주 | 필수 | 백로그 |
| 전체 흐름 QA + 데모 데이터 세팅 | — | 4주 | 필수 | 백로그 |
| 사장님 인터뷰/설문 (문제 검증) | — | 병행 | 여유 | 백로그 |

¹ 멘토링에서 조기 도입 불필요 의견 확인 — 기존 계획대로 4주차(코어·배포 이후) 유지. [6절](#6-멘토링-반영-2026-07-13) 참고.

세부 체크리스트(화면별, 항목별)는 [checklist.md](./checklist.md)에 그대로 있다 — 여기 표는 그걸 Task 단위로 묶은 요약이다.

---

## 4. 4주 로드맵 한눈에

| 주차 | 무게중심 | 목표 |
|---|---|---|
| 1주 | 데이터+루브릭 변환 / UI 목업 코딩 | 가장 어려운 것(루브릭 변환)과 전체 화면 뼈대 |
| 2주 | 백엔드 코어(A+B) + UI 컴포넌트화·mock→API 교체 (요일별 일정) | 화면과 API/콘솔 레벨 코어가 이번 주 안에 함께 돈다 |
| 3주 | UI 라우팅 골격 + 신뢰성 + 디테일 | 사장님/알바 경로가 분리되고, 평가가 믿을 만해지고, 에러 상황에도 안 깨짐 |
| 4주 | 리포트·MCP 전환·배포·QA | 완결된 v1이 배포된 URL로 돌아간다 |

---

## 5. 다음 주(2주차)까지 끝낼 것

> 1주차 이월분 + 원래 2주차 목표를 합치면 11개 Task(8개 필수)였다. 처음엔 UI·eval 작업을 3주차로 통째로 미루려 했으나, 요일별로 나눠보니 5일 안에 전부 소화 가능하다고 판단해 **Notion 백로그 표에 요일별 일정(7/13~7/17)으로 재배분**했다 (checklist.md 2주차 섹션과 동일). UI 화면 라우팅 골격(권한 분리)과 Railway/Supabase/Vercel 배포만 3주차로 남긴다.

**2주차 요일별 일정**

| 날짜 | Task | 우선순위 | 상태 |
|---|---|---|---|
| 7/13(월) | DB 마이그레이션 실행 (6개 테이블, Supabase로 전환 후 재적용 필요*) | 필수 | 완료 |
| 7/13(월) | 매장 링크 발급/조회 API (`POST /api/stores`, `GET /api/stores/:linkKey`) | 필수 | 완료 |
| 7/14(화) | 규칙 → 루브릭 변환 LLM 호출 (Sonnet 5, JSON 스키마 강제) | 필수 | 백로그 |
| 7/15(수) | eval 셋 구축 (규칙-루브릭 쌍 10~15개) | 중요 | 백로그 |
| 7/16(목) | 손님 에이전트 LLM 호출 (Haiku 4.5, 페르소나+상태 주입, 분기) | 필수 | 백로그 |
| 7/16(목) | UI 8개 화면 React 컴포넌트화 (mock 데이터) | 필수 | 백로그 |
| 7/17(금) | 평가 에이전트 LLM 호출 (Sonnet 5, JSON 출력 강제) | 필수 | 백로그 |
| 7/17(금) | 두 에이전트 턴 루프 연결 + 재입력 루프 | 필수 | 백로그 |
| 7/17(금) | 훈련 세션 DB 저장 | 필수 | 백로그 |
| 7/17(금) | UI mock → 실제 API 교체 (규칙입력·루브릭승인·매장링크전달·훈련대화) | 필수 | 백로그 |

\* 2026-07-13 과제 요구사항에 맞춰 DB 제공자를 Railway Postgres → Supabase로 전환 (Express 백엔드는 Railway 유지). 상세 경위는 [db-migration-plan.md](./db-migration-plan.md) 참고.

**3주차로 남는 것**
- [ ] UI 화면 라우팅 골격 (사장님/알바 경로 분리)
- [ ] Railway(BE)/Supabase(DB)/Vercel(FE) 빈 껍데기 배포

**2주차 완료 기준**: 매장 링크로 규칙을 저장하면 루브릭이 생성되고, 시나리오 대화 API를 몇 턴 호출하면 손님 응답과 평가(통과/미통과 + 개선문장)가 실제로 JSON으로 돌아온다. 동시에 8개 화면이 React 컴포넌트로 존재하고 mock 대신 실제 API에 연결되어 있다.

---

## 6. 멘토링 반영 (2026-07-13)

고스트캠프 멘토링(멘토: 지웅일)에서 받은 피드백 중 이 프로젝트에 해당하는 액션 아이템을 반영한다.

- **MCP 도입 보류** — 현재 서비스는 사장님/알바가 직접 쓰는 웹서비스 구조라, 외부 AI 툴(Claude·Cursor 등)이 이 서비스를 호출할 니즈가 없다는 의견. 기존 계획(코어+배포가 끝나기 전엔 MCP에 손대지 않는다, plan.md 5-6)과 이미 일치 — 조기 도입하지 않기로 재확인.
- **사장님 인터뷰/설문으로 문제 검증** — 사업화가 아니라 포트폴리오 목적의 4주 챌린지이므로, 검증보다 기능 개발을 우선하는 게 맞다는 의견. 다만 시간이 남으면 가볍게 진행할 수 있도록 Task 표에 "병행" 트랙으로 추가 (특정 주차에 고정하지 않음, 여유 우선순위).
- **AI 평가 결과 교차검증 루프** — 평가 에이전트(Claude Sonnet 5)의 채점 결과를 GPT 등 다른 모델로 한 번 더 검증하는 방법 제안받음. 기존 3주차 "평가 에이전트 캘리브레이션(사장님 검토)"이 사람에 의한 검증이라면, 이건 자동화된 모델 간 교차검증 — 두 방식을 병행하는 방향으로 3주차 Task에 별도 항목으로 추가.
